:change default browser
sudo update-alternatives --config x-www-browser

:make executable
sudo chmod +x filepath

:add user
adduser <username>

:adduser to group

:shows dir space available and used
df -Th /directory

:fix windows headers for py files
sed -i 's/\r$//' file.py
:check header change
head -1 file.py | cat -A
(ideal output) #!/usr/bin/env python3$

:set up timed events
cron

:write permissions
chmod u+w filename
chmod 755 dirpath

:basic secure shell connect
ssh username@ipadress

:distro logo and info ASCII
screenfetch

:change hostname
sudo hostnamectl set-hostname <new_hostname>
